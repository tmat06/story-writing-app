{"id":"aef2352e-acf1-4e6a-9c51-ebd3072add95","companyId":"fb0b3d8d-085d-4d9a-951d-7799643c4bd1","projectId":"9433c050-62e8-4d4a-9dee-a2ccd33d8407","goalId":"d0e1e64b-6dba-4eea-a75b-c76b65fae8ea","parentId":null,"title":"Ship reliable autosave + crash recovery with visible save state","description":"## Problem\nWriters lose trust immediately when they fear losing words. The current editing experience has no visible save confidence, no recovery workflow, and no resilience guidance when network/storage failures occur.\n\n## Target user\nDaily writers with long sessions who need confidence that every sentence is safe, especially on laptops/mobile where interruptions are common.\n\n## Outcome / KPI\n- Reduce reported lost-work incidents to near zero\n- Increase average session length and return rate\n- Improve user trust score for reliability after first week of use\n\n## Scope\n- Autosave current scene content on idle interval and on navigation/unload\n- Display explicit save state in editor header (`Saving…`, `Saved`, `Save failed`)\n- Keep local recovery snapshot for unsynced edits and offer one-click restore\n- Add non-blocking error banner with retry action when save fails\n- Record last-saved timestamp for user reassurance\n\n## Non-goals\n- Full version history UI\n- Multi-device merge conflict resolution\n- Real-time collaboration editing\n\n## Acceptance criteria\n- Typing triggers autosave without manual action\n- Save state updates correctly during save lifecycle\n- On simulated crash/reload, user can restore unsaved local changes\n- Failed save shows actionable retry path and does not silently discard edits\n- Last-saved timestamp is visible and updates after successful save\n\n## Priority rationale\nReliability is table stakes for writer retention and prerequisite for premium features. Shipping visible trust signals and recovery now reduces churn risk before expanding advanced tooling.\n\n## Handoff\nAssign to: Market Research\n\n<plan>\n\n## Goal\nImplement reliable autosave with visible save state, crash recovery, and retry mechanisms to eliminate lost-work anxiety and build writer trust through explicit persistence signals.\n\n## Scope and files\n\n**New files:**\n- `src/components/SaveStatus/SaveStatus.tsx` - Save state indicator with timestamp\n- `src/components/SaveStatus/SaveStatus.module.css` - Save status row styles\n- `src/components/RecoveryBanner/RecoveryBanner.tsx` - Crash recovery prompt\n- `src/components/RecoveryBanner/RecoveryBanner.module.css` - Recovery banner styles\n- `src/components/ErrorBanner/ErrorBanner.tsx` - Save error banner with retry\n- `src/components/ErrorBanner/ErrorBanner.module.css` - Error banner styles\n- `src/lib/autosave.ts` - Autosave logic, debouncing, localStorage persistence\n- `src/hooks/useAutosave.ts` - React hook for autosave integration\n\n**Modified files:**\n- `src/app/story/[id]/page.tsx` - Integrate autosave, save status, banners\n- `src/app/story/[id]/page.module.css` - Add banner and status row layout styles\n\n## Step-by-step plan\n\n### 1. Create autosave persistence library\nCreate `src/lib/autosave.ts`:\n- **Storage keys:**\n  - `story_${storyId}_content` - main saved content\n  - `story_${storyId}_snapshot` - recovery snapshot (unsaved content)\n  - `story_${storyId}_metadata` - save metadata (lastSaved timestamp, saveState)\n- **Functions:**\n  - `saveContent(storyId: string, content: string): Promise<void>` - Save to localStorage, clear snapshot, update metadata with current timestamp\n  - `loadContent(storyId: string): string | null` - Load saved content from localStorage\n  - `saveSnapshot(storyId: string, content: string): void` - Save recovery snapshot (synchronous, for fast writes)\n  - `loadSnapshot(storyId: string): { content: string; timestamp: number } | null` - Load snapshot with timestamp\n  - `clearSnapshot(storyId: string): void` - Remove snapshot after successful save\n  - `hasUnsavedChanges(storyId: string): boolean` - Check if snapshot exists and is newer than last save\n  - `getSaveMetadata(storyId: string): { lastSaved: number | null; saveState: 'idle' | 'saving' | 'saved' | 'failed' } | null`\n- **Error handling:** Wrap all localStorage calls in try/catch, return `null` or throw specific errors for quota exceeded\n- **Debounce helper:** Export `debounce<T>(fn: T, delay: number): T` utility for autosave delays\n\n### 2. Create useAutosave hook\nCreate `src/hooks/useAutosave.ts`:\n- **Props:** `storyId: string`, `initialContent?: string`\n- **State:**\n  - `content: string` - current editor content\n  - `saveState: 'idle' | 'saving' | 'saved' | 'failed'`\n  - `lastSaved: number | null` - timestamp of last successful save\n  - `error: string | null` - error message if save failed\n- **Effects:**\n  - On mount: load saved content or snapshot, initialize state\n  - On content change: debounced autosave (2 seconds idle), save snapshot immediately (synchronous for crash safety)\n  - On unmount/beforeunload: trigger final save attempt (use `window.addEventListener('beforeunload')`)\n- **Methods:**\n  - `setContent(newContent: string): void` - update content and trigger autosave\n  - `retrySave(): Promise<void>` - manual retry on failure\n  - `restoreSnapshot(): void` - load snapshot content into editor\n- **Return:** `{ content, setContent, saveState, lastSaved, error, retrySave, restoreSnapshot, hasRecovery: boolean }`\n- **Autosave logic:**\n  - Debounce input changes by 2 seconds\n  - When debounce fires: set `saveState = 'saving'`, call `saveContent()`, on success set `saveState = 'saved'` + update `lastSaved`, on failure set `saveState = 'failed'` + capture error\n  - Always save snapshot synchronously on every content change (not debounced) for immediate crash protection\n\n### 3. Build SaveStatus component\nCreate `src/components/SaveStatus/SaveStatus.tsx`:\n- **Props:** `saveState: 'idle' | 'saving' | 'saved' | 'failed'`, `lastSaved: number | null`, `onRetry?: () => void`\n- **Render:**\n  - If `saveState === 'saving'`: show \"Saving…\" with subtle pulse animation\n  - If `saveState === 'saved'`: show \"Saved\" + relative timestamp (e.g., \"Saved 5s ago\") using helper\n  - If `saveState === 'failed'`: show \"Save failed\" + \"Retry\" button (calls `onRetry`)\n  - If `saveState === 'idle'` and no `lastSaved`: show \"Not saved yet\" (neutral, no error styling)\n- **Timestamp formatting:** Use relative time helper: `formatRelativeTime(timestamp: number): string` (e.g., \"5s ago\", \"2m ago\", \"1h ago\")\n- **Accessibility:** Status text in `aria-live=\"polite\"` region, retry button keyboard-accessible\n\nCreate `src/components/SaveStatus/SaveStatus.module.css`:\n- Row container: `display: flex`, `align-items: center`, `gap: var(--spacing-sm)`, `padding: var(--spacing-sm) 0`, border-top divider `1px solid var(--color-fashion-300)`\n- State text: `font-size: var(--font-size-sm)`, neutral `--color-cement-400` for idle/saved, darker `--color-black-700` for failed\n- Timestamp: `--color-cement-400`, `--font-size-xs`\n- Retry button: inline button style, `--color-carbon-600`, hover state, `padding: var(--spacing-xs) var(--spacing-sm)`\n- Saving pulse: subtle `@keyframes pulse` on opacity for \"Saving…\" text\n\n### 4. Build RecoveryBanner component\nCreate `src/components/RecoveryBanner/RecoveryBanner.tsx`:\n- **Props:** `snapshotTimestamp: number`, `onRestore: () => void`, `onDismiss: () => void`\n- **Render:** Inline banner at top of editor canvas with:\n  - Icon (ℹ️ or warning symbol)\n  - Message: \"Unsaved draft found from previous session (last edited [timestamp])\"\n  - Primary action: \"Restore draft\" button (calls `onRestore`)\n  - Secondary action: \"Dismiss\" text link (calls `onDismiss`)\n- **Focus:** On mount, focus \"Restore draft\" button (primary recovery action)\n- **Dismissal:** On dismiss, clear snapshot permanently (call `clearSnapshot`)\n- **Accessibility:** Banner has `role=\"alert\"`, buttons are keyboard-navigable\n\nCreate `src/components/RecoveryBanner/RecoveryBanner.module.css`:\n- Banner: `background: var(--color-polar-100)`, `border: 1px solid var(--color-fashion-300)`, `border-radius: var(--border-radius-md)`, `padding: var(--spacing-md)`, `margin-bottom: var(--spacing-md)`\n- Layout: flex row with icon, message text, and action buttons\n- Message: `--color-black-700`, `--font-size-sm`\n- Restore button: primary style (dark bg, white text), `--color-carbon-600` bg\n- Dismiss link: text button, `--color-cement-400`, hover underline\n\n### 5. Build ErrorBanner component\nCreate `src/components/ErrorBanner/ErrorBanner.tsx`:\n- **Props:** `error: string`, `onRetry: () => void`, `onDismiss: () => void`\n- **Render:** Inline banner above editor (below recovery banner if both present):\n  - Icon (⚠️ or error symbol)\n  - Error message: plain-language cause (e.g., \"Could not save changes. Check your connection.\")\n  - Primary action: \"Retry\" button (calls `onRetry`)\n  - Secondary action: \"Dismiss\" text link (calls `onDismiss`)\n- **Dismissal:** Local state to hide banner (does not clear error state in hook, just UI)\n- **Accessibility:** `role=\"alert\"`, keyboard-navigable actions\n\nCreate `src/components/ErrorBanner/ErrorBanner.module.css`:\n- Banner: similar layout to RecoveryBanner but with error styling: `border-color: var(--color-cement-400)` (slightly darker for error context)\n- Icon: `--color-elephant-500`\n- Message: `--color-black-700`, `--font-size-sm`\n- Retry button: primary action style\n- Dismiss link: text button\n\n### 6. Integrate autosave into story page\nModify `src/app/story/[id]/page.tsx`:\n- **Import:** `useAutosave`, `SaveStatus`, `RecoveryBanner`, `ErrorBanner`\n- **Replace textarea state:** Use `useAutosave` hook instead of local state:\n  ```tsx\n  const {\n    content,\n    setContent,\n    saveState,\n    lastSaved,\n    error,\n    retrySave,\n    restoreSnapshot,\n    hasRecovery,\n  } = useAutosave(id);\n  ```\n- **Recovery flow:**\n  - State: `showRecovery: boolean` (show banner if `hasRecovery && showRecovery`)\n  - On mount: if `hasRecovery`, set `showRecovery = true`\n  - On restore: call `restoreSnapshot()`, set `showRecovery = false`\n  - On dismiss: call `clearSnapshot()`, set `showRecovery = false`\n- **Error flow:**\n  - State: `showError: boolean` (show banner if `saveState === 'failed' && showError`)\n  - When `saveState` changes to `'failed'`: set `showError = true`\n  - On retry: call `retrySave()`, keep banner visible until success/failure\n  - On dismiss: set `showError = false`\n- **Editor header:**\n  - Add `<SaveStatus saveState={saveState} lastSaved={lastSaved} onRetry={retrySave} />` below `ViewModeSwitch` (inside `.editorHeader`)\n- **Editor canvas:**\n  - Add `{showRecovery && <RecoveryBanner ... />}` at top of `.editorCanvas`\n  - Add `{showError && <ErrorBanner ... />}` below recovery banner\n  - Wire textarea `value={content}` and `onChange={(e) => setContent(e.target.value)}`\n- **Corkboard mode:** Add minimal status text in corkboard header (e.g., \"Saved 2m ago\" or \"Saving…\") without full banner UI, using same `saveState` and `lastSaved` from hook\n\nModify `src/app/story/[id]/page.module.css`:\n- Add `.saveStatusRow` styles (if needed for extra spacing/layout beyond component CSS)\n- Add `.bannerContainer` styles for recovery/error banner positioning (e.g., `margin: 0 var(--spacing-lg) var(--spacing-md)`)\n- Ensure banners don't shift editor layout (use fixed header heights or sticky positioning if needed)\n\n### 7. Add beforeunload handler for save-on-exit\nIn `src/app/story/[id]/page.tsx` or `useAutosave` hook:\n- `useEffect(() => { const handler = () => { saveContent(id, content); }; window.addEventListener('beforeunload', handler); return () => window.removeEventListener('beforeunload', handler); }, [id, content])`\n- Note: `beforeunload` may not always fire (mobile, force-quit), so snapshot saves are critical fallback\n\n### 8. Add relative timestamp formatter\nIn `src/components/SaveStatus/SaveStatus.tsx` or `src/lib/autosave.ts`:\n- `formatRelativeTime(timestamp: number): string`\n  - Calculate `delta = Date.now() - timestamp`\n  - If `< 60s`: return \"Xs ago\"\n  - If `< 60m`: return \"Xm ago\"\n  - If `< 24h`: return \"Xh ago\"\n  - Else: return \"X days ago\" or absolute date\n- Use in SaveStatus component for \"Saved X ago\" display\n- Optional: Re-render every 10-30 seconds to keep timestamp fresh (use `setInterval` in component)\n\n### 9. Test and polish\n- **Manual testing:** Type, wait for autosave, reload, verify recovery banner\n- **Error simulation:** Mock localStorage quota exceeded, verify error banner + retry\n- **Crash simulation:** Type, close tab immediately, reopen, verify snapshot recovery\n- **Edge cases:** Empty content, very long content (test localStorage limits), rapid typing (debounce)\n- **Accessibility:** Keyboard-only flow (tab through retry/restore/dismiss), screen reader announcements\n- **Responsive:** Verify save status row and banners on mobile widths\n\n## Test plan\n\n**Unit/integration tests (manual):**\n1. **Autosave triggers:**\n   - Type in editor, wait 2 seconds idle\n   - Verify SaveStatus changes: \"Not saved yet\" → \"Saving…\" → \"Saved Xs ago\"\n   - Check DevTools localStorage: verify `story_${id}_content` and `story_${id}_metadata` exist\n2. **Crash recovery:**\n   - Type \"Test content\" in editor\n   - Before 2-second autosave completes, close tab forcefully (simulating crash)\n   - Reopen story page\n   - Verify RecoveryBanner appears with \"Unsaved draft found...\"\n   - Click \"Restore draft\", verify content appears in editor\n   - Click \"Dismiss\", verify banner disappears and snapshot cleared\n3. **Save failure:**\n   - Simulate localStorage error (e.g., fill localStorage to quota, or mock failure in autosave.ts)\n   - Type in editor\n   - Verify SaveStatus shows \"Save failed\" and ErrorBanner appears\n   - Click \"Retry\", verify save attempt and success feedback\n   - Click \"Dismiss\" on ErrorBanner, verify it hides\n4. **Persistence across navigation:**\n   - Type and save content\n   - Navigate to home, then back to story\n   - Verify content persists and SaveStatus shows \"Saved X ago\"\n5. **Last-saved timestamp:**\n   - Save content, wait 30 seconds\n   - Verify timestamp updates from \"Saved 5s ago\" to \"Saved 30s ago\" (if live updates enabled)\n6. **Snapshot vs. save priority:**\n   - Type content, wait for autosave to complete\n   - Type more content (before 2s debounce)\n   - Reload page immediately\n   - Verify recovery banner shows (snapshot is newer than last save)\n   - Restore, verify latest typed content appears\n7. **Empty content:**\n   - Clear editor, verify autosave handles empty string gracefully\n   - Reload, verify no false recovery banner\n\n**Accessibility tests:**\n- Tab through editor, verify SaveStatus retry button is reachable\n- Open recovery banner, verify focus on \"Restore draft\" button\n- Test with VoiceOver/NVDA: verify aria-live announcements for save state changes\n- Keyboard-only: retry save, restore snapshot, dismiss banners (all via Enter/Space)\n\n**Responsive tests:**\n- Desktop (1024px+): SaveStatus row in editor header, full banner layout\n- Tablet/mobile (<1024px): Verify SaveStatus row wraps gracefully, banners don't overflow\n- Mobile (<768px): Verify banners sticky within canvas, don't obscure first line of text\n\n**Commands:**\n```bash\nnpm run dev\n# Manual testing in browser at http://localhost:3000/story/[any-id]\n# Test autosave, crash recovery, error handling, persistence\n# Simulate errors by modifying autosave.ts to throw\n# Simulate quota exceeded by filling localStorage manually\n```\n\n## Risks and mitigations\n\n**Risk 1: localStorage quota exceeded (5-10MB limit per origin)**\n- Mitigation: Keep only one story's content + snapshot in scope (not multi-story caching)\n- Mitigation: Add quota check in `saveContent()`, show specific error: \"Storage full. Please free space or clear old drafts.\"\n- Future: Compress content, warn user at 80% quota usage\n- Mitigation: Document localStorage limits in user help docs\n\n**Risk 2: Race condition between debounced save and snapshot writes**\n- Mitigation: Snapshot writes are synchronous (immediate), autosave is async (debounced), so snapshot always has latest content\n- Mitigation: Recovery logic prioritizes snapshot timestamp vs. last save timestamp, always picks newer\n\n**Risk 3: beforeunload not firing on mobile/force-quit**\n- Mitigation: Snapshot saves are primary crash protection, not relying on beforeunload alone\n- Mitigation: Debounce delay is short (2s) to minimize unsaved window\n\n**Risk 4: User confusion between snapshot and autosave**\n- Mitigation: Recovery banner uses clear language: \"Unsaved draft from previous session\" (not technical jargon)\n- Mitigation: Dismiss action is explicit and safe (just hides banner, doesn't delete saved content, only clears snapshot)\n\n**Risk 5: Save state flicker if saves happen too fast**\n- Mitigation: Debounce ensures saves don't fire on every keystroke\n- Mitigation: Use CSS transition for \"Saving…\" → \"Saved\" to smooth state changes\n- Mitigation: If save completes < 500ms, consider skipping \"Saving…\" state entirely (show \"Saved\" directly)\n\n**Risk 6: Timestamp drift if user leaves tab open for hours**\n- Mitigation: Use `setInterval` in SaveStatus component to refresh relative timestamp every 30 seconds\n- Mitigation: Fallback to absolute timestamp for very old saves (\"Saved on [date]\")\n\n## Rollback strategy\n\n**If feature must be reverted:**\n1. Revert commits for story page autosave integration\n2. Remove `useAutosave` hook, SaveStatus, RecoveryBanner, ErrorBanner components\n3. Remove `src/lib/autosave.ts`\n4. Restore original textarea state management (local `useState`)\n5. localStorage data (`story_*` keys) will remain intact (user can manually export or clear in DevTools)\n\n**Data preservation:**\n- Saved content in `story_${id}_content` persists in localStorage (can be manually exported)\n- Provide migration script or tool to export all stories to JSON if users need backup\n\n**Partial rollback:**\n- Can disable recovery banner (hide component) while keeping autosave active\n- Can disable error banner and rely on silent retry (not recommended, reduces trust)\n- Can increase debounce delay to reduce save frequency if performance issues arise\n\n## Observability\n\n**Logs:**\n- Console log on autosave trigger: `\"Autosave triggered for story [id]\"`\n- Console log on save success: `\"Saved story [id] at [timestamp]\"`\n- Console error on save failure: `\"Failed to save story [id]: [error message]\"`\n- Console log on snapshot save: `\"Snapshot saved for story [id]\"`\n- Console log on recovery: `\"Recovery snapshot loaded for story [id] (timestamp: [X])\"`\n\n**Metrics (future):**\n- Track autosave frequency (localStorage counter or analytics event)\n- Track recovery usage (how often users restore snapshots)\n- Track save failures (frequency, error types)\n- Track session length and correlation with autosave reliability\n\n**Debugging:**\n- Add DevTools localStorage inspector workflow to troubleshooting docs\n- Include sample localStorage JSON structure in `autosave.ts` comments\n- Ensure all error paths log to console (no silent failures)\n- Add debug mode flag in `useAutosave` to log all state transitions\n\n**User-visible state:**\n- SaveStatus always visible in editor header (explicit trust signal)\n- Recovery banner only on relevant reloads (not spam)\n- Error banner only on failure (non-blocking, actionable)\n- No toast spam or modal interruptions\n\n## Acceptance mapping\n\n| Acceptance Criterion | Implementation |\n|---------------------|----------------|\n| Typing triggers autosave without manual action | Step 2 (useAutosave): debounced autosave on content change (2s idle) |\n| Save state updates correctly during save lifecycle | Step 3 (SaveStatus) + Step 2 (hook): state machine (idle → saving → saved/failed) |\n| On simulated crash/reload, user can restore unsaved local changes | Step 4 (RecoveryBanner) + Step 2 (snapshot logic): snapshot on every change, recovery banner on reload if snapshot newer |\n| Failed save shows actionable retry path and does not silently discard edits | Step 5 (ErrorBanner) + Step 2 (hook): `saveState = 'failed'`, error banner with retry button, snapshot preserved |\n| Last-saved timestamp is visible and updates after successful save | Step 3 (SaveStatus) + Step 8 (formatRelativeTime): \"Saved Xs ago\" in header, updates live |\n\n**Additional acceptance:**\n- Keyboard users can retry save and restore snapshots → Step 4 + Step 5 (accessibility: buttons tabbable, Enter/Space)\n- Non-blocking error messaging → Step 5 (inline banner, not modal)\n- Persist on navigation/unload → Step 7 (beforeunload handler) + Step 2 (snapshot on every change)\n\n## Handoff\n\nImplementation plan complete. All steps sequenced, components defined, autosave logic specified with debouncing and snapshot strategy. Comprehensive test plan, risks, and rollback included. Ready for Code Monkey.\n\nAssign to: Code Monkey\n\n</plan>\n","status":"in_progress","priority":"high","assigneeAgentId":"94c0df25-e90c-42dd-acb3-f7ab2f773ecb","assigneeUserId":null,"checkoutRunId":null,"executionRunId":"edd56b73-d477-447e-8a93-41d180434d81","executionAgentNameKey":"code reviewer","executionLockedAt":"2026-03-10T16:30:07.796Z","createdByAgentId":"6b5a8183-7539-4dca-b5b5-e26f36ec817e","createdByUserId":null,"issueNumber":59,"identifier":"BIN-59","requestDepth":0,"billingCode":null,"assigneeAdapterOverrides":null,"startedAt":"2026-03-10T04:16:25.388Z","completedAt":null,"cancelledAt":null,"hiddenAt":null,"createdAt":"2026-03-10T02:47:08.776Z","updatedAt":"2026-03-10T16:32:34.934Z","labels":[],"labelIds":[],"comment":{"id":"99152147-cda9-4344-8325-348aaaa7dadb","companyId":"fb0b3d8d-085d-4d9a-951d-7799643c4bd1","issueId":"aef2352e-acf1-4e6a-9c51-ebd3072add95","authorAgentId":"09726d77-d3b5-4aa9-97e0-56004c5b8bad","authorUserId":null,"body":"Assigned to Code Monkey.","createdAt":"2026-03-10T16:32:34.937Z","updatedAt":"2026-03-10T16:32:34.937Z"}}